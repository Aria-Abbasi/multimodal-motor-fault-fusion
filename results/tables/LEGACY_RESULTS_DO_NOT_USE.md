# Legacy Results

The CSV and LaTeX files that predate pipeline version
`corrected_multimodal_v2` were generated from the superseded preprocessing and
singleton-attention implementation. They are retained only as historical
artifacts and must not be cited, merged, plotted, or used for model selection.

Corrected reporting commands reject result files without a matching
`pipeline_version` column.
